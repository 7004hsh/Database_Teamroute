spring:
  datasource:
    url: jdbc:mysql://localhost:3306/teamroute?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root         # ← 본인 MySQL 사용자 이름
    password: g01049088861  # ← 실제 비밀번호 입력
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none       # 이미 DB에 테이블이 있으므로 create나 update 대신 none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
